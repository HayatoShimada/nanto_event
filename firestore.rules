rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    function isAuthenticated() {
      return request.auth != null;
    }

    function isOwner(uid) {
      return isAuthenticated() && request.auth.uid == uid;
    }

    // users
    match /users/{userId} {
      allow read: if true;
      allow create: if isOwner(userId);
      allow update: if isOwner(userId) &&
        !request.resource.data.diff(resource.data).affectedKeys().hasAny(['role']);
      allow delete: if false;
    }

    // events
    match /events/{eventId} {
      allow read: if true;
      allow create: if isAuthenticated() && request.resource.data.organizerUid == request.auth.uid;
      allow update: if isAuthenticated() && resource.data.organizerUid == request.auth.uid;
      allow update: if request.resource.data.diff(resource.data).affectedKeys().hasOnly(['pageViews', 'participationClicks']);
      allow delete: if isAuthenticated() && resource.data.organizerUid == request.auth.uid;
    }

    // event_collaborators
    match /event_collaborators/{docId} {
      allow read: if isAuthenticated();
      // Allow if the user is the organizer of the event
      allow create, update, delete: if isAuthenticated() &&
        get(/databases/$(database)/documents/events/$(request.resource.data.eventId)).data.organizerUid == request.auth.uid;
    }

    // event_participations
    match /event_participations/{docId} {
      allow read: if isAuthenticated();
      allow create: if isAuthenticated() &&
        request.resource.data.userId == request.auth.uid &&
        request.resource.data.status == 'attending';
      allow update: if isAuthenticated() &&
        resource.data.userId == request.auth.uid;
      allow delete: if false;
    }

    // mail (Trigger Email Extension)
    match /mail/{mailId} {
      allow read: if false;
      allow create: if false;
    }

    // email_templates
    match /email_templates/{templateId} {
      allow read: if false;
      allow write: if false;
    }
  }
}
